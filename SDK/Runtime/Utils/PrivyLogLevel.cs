namespace Privy
{
    public enum PrivyLogLevel
    {
        /** No logs. */
        NONE,
        DEBUG,
        INFO,
        WARNING,
        ERROR,

        /** Logs everything */
        VERBOSE,
    }
}
